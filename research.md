# research.md — 작업·조사 기록

> CLAUDE.md 규칙: 모든 질문·요구·요청과 진행 과정·결과를 신중·깊이·상세·명확·정확하게 정리해 여기에 누적 기록한다.

## 기록 형식
- **날짜 — 제목**
  - 요청:
  - 진행:
  - 결정·근거:
  - 결과:

---

## 2026-07-13 — CLAUDE.md 규칙 추가 및 전 저장소 횡전개
- 요청: CLAUDE.md에 "모든 답변을 신중·깊이·상세·명확·정확하게 정리하고 research.md에 기록" 규칙 추가, 전 저장소 기본 브랜치에 횡전개.
- 진행: 7개 저장소(axax77, axdata_01/03/05/07/09, gax)의 기본 브랜치 CLAUDE.md에 "답변·기록 규칙" 섹션 추가, 각 저장소에 research.md 생성.
- 결정·근거: '횡전개' = 새 세션이 실제로 여는 기본 브랜치에 반영(ponytail 배포와 동일 기준). 기존 CLAUDE.md는 보존하고 규칙 섹션만 추가(수술적 변경).
- 결과: 각 저장소 커밋·푸시 완료(아래 커밋 참조).

## 2026-07-13 — 제작 대상 분리 + 대상별 설정 트리 + 등급/폴더 개편 (axdata_09)
- 요청(연속): (1) 대기 애니메이션 프레임 크기 흔들림 수정 (2) 아트 요소를 6개 상위 메뉴(캐릭터/아이템/환경/특수효과/UI/세트)로 재구성 + 좌측 패널 확대·가운데 축소 (3) "캐릭터 외는 사물/디자인"이므로 제작 대상을 캐릭터로 묶지 말 것 → 제작 대상 분리 (4) 전체 코드 재점검 브리핑 후 A+B+C 반영 + 데드코드(D) 정리 (5) 세트에서 영상·BGM 제외, 시트만 (6) 아이템/환경/특수효과/UI 대상별 설정 트리(종류 드롭다운, 등급) (7) 무기→세부 종류 2단계 + 컨셉 강한 유지 프롬프트 + 저장 경로 대분류/중분류 (8) 아이템 요소는 일러스트만, 등급 10단계·고등급 강조.
- 진행:
  - 대기 애니: anim_idle 기준 1장만 AI 생성, 나머지는 발 고정 미세 호흡 절차 합성(imageops.breathe) → 프레임 크기 동일.
  - 6대분류: 카탈로그 SUPERGROUPS 정의, 선택·결과 그룹핑을 groupBySuper 한 곳으로 통일.
  - 제작 대상 분리: SUBJECTS(character/item/environment/vfx/ui), 사물은 캐릭터 기획(스탯·성격·배경) 없이 이름·묘사·visual_core만 생성(gpt_service object 경로). 상단 탭=제작 대상, 캐릭터일 때만 엔티티 하위탭·직업. 결과 화면도 대상별.
  - 코드 재점검: 3영역(백엔드/프론트/교차) 병렬 서브에이전트 리뷰 → 실사용 버그(몬스터→사물 전환 시 기본 에셋 사라짐, 사물 히스토리 라벨·썸네일), 경로 방어 _safe_path 통일, subject Literal 검증, CSV 업로드 상한, 데드코드 정리.
  - 세트: video·bgm 스펙 제거(카탈로그에서 delist → 선택·생성 불가), 시트만.
  - 대상별 트리: TYPE_LISTS(종류), 아이템/특수효과는 등급 축(RARITY_ASSETS). item_art·vfx_art 신규.
  - 무기 세부: ITEM_SUBTYPES 2단계 드롭다운. 컨셉 강한 유지 프롬프트(_is_object) + GPT visual_core 강화.
  - 저장 경로: 대분류/중분류/장르_이름_날짜. type_group 필드, 히스토리 재귀 스캔, 엔드포인트 {job_id:path}+_safe_job, 프론트 jobPath().
  - 아이템 정리: weapon_icons/item_grid/skill_icons/rarity_frame 제거 → item_art만. 등급 10단계(일반/고급/희귀/영웅/전설/초월/멸망/태초/무아/극), 고등급일수록 외형·프레임 강조.
- 결정·근거: 대상별로 설정·산출물이 근본적으로 다르므로 최상위를 제작 대상으로 분리(사물에 캐릭터 기획·일관성은 무의미). 등급을 변형 축으로 삼아 "원하는 등급만" 선택 생성. 폴더 대분류/중분류로 산출물 관리. 영상/BGM은 코드에 남기되 카탈로그에서 delist(재활성 용이). 데드코드 중 세션 내 생성분만 제거, 기존 데드코드는 지시받고 정리.
- 결과: 스모크 테스트 51개 통과. 각 단계 커밋·푸시(feature+main). 데모/브라우저로 각 기능 검증. 실 등급별 외형 차이는 실 API에서 뚜렷.

## 2026-07-15 — '이전 버전 보임' 원인 점검(롤백 아님) + 캐시 무효화
- 요청: 아트팩 메뉴·옵션이 이전 버전 같다, 롤백된 적 있는지 확인.
- 진행: git 이력·현재 코드 점검 → 롤백/revert/reset 없음, 최신 기능(제작대상5·등급10·item_art·세부종류·등급색·영상BGM제거) 모두 코드에 존재. 원인=브라우저 캐시(index.html 이 app.js/style.css 를 버전 없이 로드). index() 라우트에서 파일 mtime 을 ?v= 로 주입해 캐시 무효화 추가.
- 결정·근거: 서버는 최신이 맞으므로 롤백 아님. 재발 방지로 캐시버스팅(수정시각 버전). 즉시 해결은 강력 새로고침(Ctrl+F5).
- 결과: index 응답에 app.js?v=·style.css?v= 주입 확인, 버전 URL 200. 테스트 추가·통과. 커밋·푸시.

## 2026-07-13 — 결과 카드 등급 배지 + 특수효과 세부 종류 2단계
- 요청: (다음 안건) 결과 카드 등급 배지 · 특수효과 세부 종류 2단계.
- 진행: GeneratedAsset.variant 추가→결과 카드 좌상단에 등급색 배지(rarityBadge). RARITY_COLORS 단일 소스로 프레임·배지 색 통일(imageops 가 카탈로그 색 참조). VFX_SUBTYPES(화염→폭발/화염구 등) 추가·SUBTYPES 병합→특수효과도 종류→세부 2단계+등급.
- 결정·근거: 색은 한 곳(RARITY_COLORS)에서 관리해 프레임·배지 드리프트 방지. 세부 종류는 종류 값이 유일하므로 통합 dict 유지.
- 결과: 스모크 55개 통과. 브라우저로 배지색·vfx 세부 검증. 커밋·푸시.

## 2026-07-13 — 소통 규칙 추가: 의사결정은 체크 선택박스로(항상)
- 요청: 의사결정이나 복수 의견을 물을 때는 항상 체크 선택박스로 띄울 것.
- 진행: CLAUDE.md 소통 규칙에 5번 항목 추가(결정·복수 선택지·의견·방향 제시 시 항상 AskUserQuestion 사용, 권장안은 첫 번째+`(권장)`).
- 결정·근거: 세션이 바뀌어도 지켜지도록 자동 로드되는 CLAUDE.md에 규칙화(훅이 아닌 판단 규칙이므로 CLAUDE.md가 적합).
- 결과: 커밋·푸시. 이후 모든 결정 질문은 선택박스로 진행.

## 2026-07-13 — 시스템 상태점검 + 기능개선 3종 (axdata_09)
- 요청: 시스템 상태 확인 후 기능 개선. (선택) 히스토리 트리 뷰 · 등급 프레임 후처리 · 환경·UI 세부 종류 2단계.
- 진행:
  - 상태점검: 원격 동기화 0/0, 테스트 통과, 라이브 서버 200, 데모 모드(키 없음), 디스크 여유 30G. 점검 중 다른 세션이 올린 CLAUDE.md 규칙+research.md를 fast-forward/merge 로 반영.
  - 히스토리 트리: renderHistoryList 를 저장 폴더 구조(대분류▸중분류) 접기/펼치기 트리로 재구성. 옛 평면 기록은 '미분류' 그룹. CSS(.hist-grp/.hist-sub) 추가.
  - 등급 프레임 후처리: imageops.apply_rarity_frame(등급별 색·굵기 테두리, 고등급=굵은 이중선). 파이프라인에서 item_art·vfx_art 생성 후 등급별로 적용 → AI 결과와 무관하게 등급 차이 항상 노출.
  - 환경·UI 세부 종류: ENV_SUBTYPES·UI_SUBTYPES 정의, ITEM/ENV/UI 통합 SUBTYPES. 프론트 renderSubtypes 를 대상 무관·종류 기반으로 일반화. type_group(종류/테마)을 GPT 브리핑·데모 visual 에 주입해 세부와 함께 반영.
- 결정·근거: 최근 만든 중첩 폴더 구조를 히스토리에서 실제로 활용(트리). 등급은 프롬프트만으론 불확실 → 후처리로 '꼭 지켜지도록' 보장(데모에서도 확인 가능). 세부 종류는 종류 값이 유일하므로 한 dict 로 통합(단순).
- 결과: 스모크 54개 통과. 브라우저·백엔드로 3종 검증(전설 프레임 색 정확·환경/UI 세부·트리 그룹). 커밋·푸시(feature+main).

## 구현 현황·백로그 (CLAUDE.md에서 이전, 2026-07)
> CLAUDE.md 슬림화를 위해 자주 바뀌는 현황·백로그를 여기로 옮김.

### 현재 구현 현황  (2026-07-13 갱신 — 위 세션 엔트리 참조)
- 제작 대상 5종: 캐릭터(캐릭터/몬스터/NPC)·아이템·환경·특수효과·UI. 대상마다 설정 트리가 다르게 노출(캐릭터=직업, 아이템=종류(무기→세부)+등급, 환경/UI=종류, 특수효과=종류+등급). 캐릭터만 캐릭터 기획(스탯·성격) 생성.
- 생성 모드 3종: 단일 생성 · 일괄(도감) · CSV/스프레드시트 가져오기.
- 아트 에셋: 캐릭터 아트·애니메이션(대기/걷기/공격)·아이템 일러스트(등급 10단계)·환경(배경/타일셋/스카이박스)·특수효과(등급 차등)·UI/HUD·통합 산출물(시트). 6개 상위 메뉴로 그룹.  ※세트에서 영상·BGM 제외(코드에 비활성 보존).
- 등급 10단계(아이템/특수효과): 일반/고급/희귀/영웅/전설/초월/멸망/태초/무아/극 — 선택한 등급만 생성, 고등급일수록 외형·프레임 강조.
- 품질 옵션: 캐릭터 일관성(사물엔 숨김)·투명 배경·스프라이트 시트·스타일 락·이미지 모델(Gemini/gpt-image-1).
- 결과 편집: 재생성(↻)·이미지 편집(✎)·변형 비교·채택 재패킹(⊞).
- 운영: 실시간 진행률+취소·예상 파일수/용량/비용·예산 경고·사용량 집계·히스토리(설정 복원)·프리셋·저장 경로.
- 저장 경로: `대분류/중분류/장르_이름_날짜` (캐릭터=엔티티/직업, 사물=제작대상/종류).
- 경로 규칙: 상대경로는 as_posix()(슬래시)로 생성.
- 주요 API: /api/generate /api/generate_batch /api/import/{preview,run} /api/template.csv /api/regenerate /api/edit /api/repack /api/budget /api/progress /api/cancel.
- 테스트: tests/test_smoke.py.
- 개발 브랜치: claude/work-prep-status-check-axn3lj → main FF 병합.
- Windows 실행: start.bat/stop.bat/update.bat, 본체 _server.bat, 숨김 _hidden.vbs.

### 남은 후보(백로그)
- C. 실 API 실사용 점검 · D. 에셋별 프롬프트 고급 편집.
- 보류: run_pipeline 헬퍼 추출(P3-9) · imageio-ffmpeg extras 분리(P4-12).
