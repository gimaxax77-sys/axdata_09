# 작업 규칙 (Gim)

> LLM의 흔한 코딩 실수를 줄이기 위한 지침입니다. 신중함을 속도보다 우선합니다.
> 사소한 작업에는 상식적으로 판단합니다.

## 소통 규칙 (반드시 준수)

1. **어체**: Gim과의 모든 작업·질문·답변은 항상 **정중하고 예의 있는 존댓말**로 합니다. 반말·편한 말투는 사용하지 않습니다.
2. **설명 방식**: 모든 설명은 **초보자 입장**에서 정리합니다.
   - 전문 용어를 쓸 때는 쉬운 말로 풀어서 함께 설명합니다.
   - 결론을 먼저 말하고, 그다음 이유와 근거를 친절하게 브리핑합니다.
   - 필요하면 비유·예시·표·그림(아티팩트)으로 눈에 보이게 정리합니다.
3. **호칭**: 사용자는 **Gim** 으로 부릅니다.
4. **한글 문장 종결**: 한글 문장은 콜론(`:`)이 아니라 **마침표**로 끝냅니다. 다음 줄이 목록·예시여도 콜론으로 끝맺지 않습니다. (코드·키값·라벨 안의 콜론은 예외.)

## 작업 방식 (실수 줄이기 9원칙)

1. **코딩 전에 생각합니다** — 가정은 명시하고, 불확실하면 물어봅니다. 해석이 여러 개면 조용히 하나를 고르지 말고 함께 제시합니다. 더 단순한 방법이 있으면 말하고, 필요하면 반대 의견을 냅니다.
2. **단순함 우선** — 문제를 푸는 최소 코드만 씁니다. 요청 안 한 기능·추상화·설정 유연성·불가능한 예외 처리는 넣지 않습니다. 200줄이 50줄로 줄 수 있으면 다시 씁니다.
3. **수술적 변경** — 꼭 고쳐야 할 곳만 건드립니다. 주변 코드·주석·포맷을 임의로 "개선"하지 않고 기존 스타일을 따릅니다. 내 변경이 만든 미사용 코드만 정리하고, 기존 데드코드는 지시 없이 지우지 않습니다(발견 시 언급만).
4. **목표 기반 실행** — 작업을 검증 가능한 목표로 바꿉니다. "검증 추가"→"잘못된 입력 테스트를 짜고 통과시키기", "버그 수정"→"재현 테스트를 짜고 통과시키기". 다단계 작업은 간단한 계획(각 단계 → 확인 방법)을 먼저 적습니다.
5. **새 파일 첫 줄 한글 헤더 주석** — 새 소스 파일은 첫 줄에 역할을 한 줄 한글 주석으로 답니다(`# 사용자 인증 상태 관리`, `-- 일별 집계 뷰`). 지시어 바로 아래에 둡니다. 설정 파일은 예외.
6. **비자명 작업엔 계획·체크리스트·컨텍스트 노트** — 규모 있는 작업은 (1) 무엇을 왜 만드는지 계획, (2) 체크박스 할 일, (3) 결정과 근거 기록을 남깁니다. 다음 세션이 다시 추론하지 않고 이어가도록 합니다.
7. **완료 전 테스트 실행** — 코드를 건드렸으면 "완료"라고 말하기 전에 테스트를 돌립니다(`python -m pytest`). 통과하면 결과 보고, 실패하면 고치고 재실행. Gim이 "끝/완료"라고 하기 전에 미리 돌립니다.
8. **의미 단위 커밋** — 논리적 변경 하나가 끝나면 커밋합니다. "한 문장으로 설명되면" 커밋, 아니면 섞인 것이니 나눕니다. 되돌리기 가능하도록 유지합니다.
9. **에러는 추측 말고 읽습니다** — 실패하면 전체 에러·스택·실제 로그를 읽습니다. 원인 확인 전에 "흔한 해결책"을 먼저 적용하지 않습니다. 불명확하면 로그를 찍어 상태를 확인한 뒤 고칩니다.

## 프로젝트 개요

- **AXData Studio** — GPT(기획) + Gemini(아트, Nano Banana) + CapCut(영상)을 조합해 **캐릭터·몬스터·NPC** 게임 아트 에셋을 자동 제작하는 웹 툴. Python.
- 스택: **FastAPI + uvicorn** 백엔드, **바닐라 JS/HTML/CSS** 프론트(빌드 단계 없음), **Pillow** 이미지 처리.
- 실행: `start.bat`(Windows 원클릭) 또는 `uvicorn app.main:app --reload` → http://127.0.0.1:8000
- **데모 폴백**: API 키(`OPENAI_API_KEY`/`GEMINI_API_KEY`)가 없으면 로컬 플레이스홀더로 전 파이프라인이 끝까지 동작합니다.
- 결과물: `OUTPUT_DIR` 아래 `<엔티티>_<이름>_<날짜시각>/` 폴더로 자동 저장.
- 구조: `app/main.py`(FastAPI 라우트) · `app/services/`(오케스트레이션·서비스) · `app/static/`(웹 UI) · `tests/`(스모크 테스트).
- 아트 연결점(단일 진실 공급원): **`app/services/asset_catalog.py`** — 아트 요소를 여기 한 곳에서 정의하면 GPT 프롬프트·Gemini 생성·UI 체크리스트에 자동 반영됩니다.

## 코드 작성 원칙

- 아트 에셋은 **`asset_catalog.py` 한 곳에서만** 정의합니다(추가 시 전 계층 자동 반영).
- 모든 외부 호출(GPT/Gemini)은 **실패해도 데모 폴백으로 끝까지 동작**해야 합니다.
- 로깅은 `app/logging_config.py` 의 `get_logger` 를 씁니다(`print` 금지).
- 서비스는 역할별 모듈로 나눕니다(`pipeline`·`gpt_service`·`gemini_service`·`importer`·`editor`·`usage`·`progress` 등).
- 로컬 제어 엔드포인트(설정/폴더 열기/예산 등)는 `_require_local` 로 루프백만 허용합니다.
- 테스트: `python -m pytest` (스모크 테스트 `tests/test_smoke.py`).

## 현재 구현 현황 (2026-07 기준)

- **생성 모드 3종**: 단일 생성 · 일괄(도감) · **CSV/스프레드시트 가져오기**(용도 기반 에셋 자동 설정).
- **아트 에셋 30+종**: 초상화·전신·표정·포즈·도트·턴어라운드·엠블럼·이모트·탈것, 아이템/스킬 아이콘, 배경·타일셋·스카이박스, **특수효과(VFX: 속성/스킬/타격)**, 로고·카드·배너·스플래시·프로필카드, 인게임 UI/HUD, 애니메이션(대기/걷기/공격)·시트·영상·**BGM**. 변형 개수 선택(3/5/7/10).
- **품질 옵션**: 캐릭터 일관성(레퍼런스) · 투명 배경(알파) · 스프라이트 시트 패킹 · 스타일 락 · 이미지 모델 선택(Gemini/gpt-image-1).
- **결과 편집**: 개별 에셋 재생성(↻) · 이미지 편집(✎ 크롭·배경·밝기/대비/채도) · 변형 비교·채택 재패킹(⊞).
- **운영**: 실시간 진행률+취소 · 예상 파일수/용량/비용 표시 · 비용 확인·예산 경고(1회/월 상한) · 사용량 집계 · 생성 히스토리 · 프리셋 저장 · 저장 경로 설정. 폴더명은 `장르_직업_캐릭명_날짜`.
- **경로 규칙**: 모든 상대경로는 `as_posix()`(슬래시)로 생성 — Windows 역슬래시로 인한 UI 경로 깨짐 방지.
- **주요 API**: `/api/generate` `/api/generate_batch` `/api/import/{preview,run}` `/api/template.csv` `/api/regenerate` `/api/edit` `/api/repack` `/api/budget` `/api/progress` `/api/cancel`.
- **테스트**: `tests/test_smoke.py` (파이프라인·배경제거·타일러블·경로안전·카탈로그·진행률/취소·재생성·재패킹·편집·예산·CSV가져오기).
- **개발 브랜치**: `claude/work-prep-status-check-axn3lj` → `main` 으로 fast-forward 병합해 반영.
- **Windows 실행**: `start.bat`(백그라운드 실행, 로그 server.log) · `stop.bat`(종료) · `update.bat`(코드+패키지 갱신). 서버 본체는 `_server.bat`, 숨김 실행은 `_hidden.vbs`.

## 남은 후보 (선택)

- C. 실 API 실사용 점검(키 넣고 진행률/취소/재생성/비용 실동작 확인) · D. 에셋별 프롬프트 고급 편집.
- 보류: `run_pipeline` 헬퍼 추출(P3-9) · imageio-ffmpeg extras 분리(P4-12).

---

**이 규칙이 잘 지켜지면**: diff에 불필요한 변경이 줄고, 과설계로 인한 재작성이 줄며, 실수 후가 아니라 구현 전에 질문이 나옵니다.
